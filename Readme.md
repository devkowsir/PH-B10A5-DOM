# Setup

```sh
git clone https://github.com/devkowsir/PH-B10A5-DOM
cd PH-B10A5-DOM

# Vscode live server extension is used in development mode with tailwindcss global executable package.
npm i -g tailwindcss
tailwindcss -w -i index.css -o style.css
```
